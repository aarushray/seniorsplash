rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Example: Allow read/write only if the request is from a signed-in user
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
